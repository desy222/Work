
/**Function which gives the higher between two numbers */
function printMax(x,y) {
    var max;
    if (x < y) {
        max=y;
    }
    console.log('Maximal number:' + max);
}
printMax(20,30);